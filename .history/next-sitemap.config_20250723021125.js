/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://shahnawaz.buttnetworks.com/", // Replace with your actual URL
  generateRobotsTxt: true,           
  sitemapSize: 5000,
  changefreq: "daily",
  priority: 0.7,
};

module.exports = config;
