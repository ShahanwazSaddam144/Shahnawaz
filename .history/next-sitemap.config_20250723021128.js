/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://shahnawaz.buttnetworks.com/",
  generateRobotsTxt: true,
  sitemapSize: 5000,
  changefreq: "daily",
  priority: 0.7,
};

module.exports = config;
